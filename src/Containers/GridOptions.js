const columnTypes = {
    valueColumn: {
        width: 150
    }
};

const defaultColDef = {
    sortable: false,
    resizable: true
}

const rowSelection = "single";

export default {
    columnTypes,
    defaultColDef,
    rowSelection
}